[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "verilm-rs"
version = "0.1.0"
requires-python = ">=3.9"
description = "Rust commitment engine for VeriLM verified inference"

[tool.maturin]
features = ["pyo3/extension-module"]
