[package]
name = "vi-test-vectors"
version = "0.1.0"
edition = "2021"

[dependencies]
vi-core = { path = "../vi-core" }
vi-prover = { path = "../vi-prover" }
rand = "0.8"
rand_chacha = "0.3"
bincode = "1"

[dev-dependencies]
vi-verify = { path = "../vi-verify" }
proptest = "1"
hex = "0.4"
